[build]
  functions = "netlify/functions"

[dev]
  framework = "astro"
  autoLaunch = true

[functions]
  node_bundler = "esbuild"

# Enable external node modules for Blobs
[functions."chat"]
  external_node_modules = ["@netlify/blobs"] 